# 黔景智作品牌与主页粒子背景并行实施 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不修改 Trae 当前 Task 8—10 文件的前提下，交付可独立合并的黔景智作品牌资源，以及只等待主页接线的 Canvas 2D 粒子显影模块。

**Architecture:** 所有工作在 `/Users/azu/Documents/guikesong-codex-parallel` 的 `codex/brand-particle-foundation` 分支完成。品牌层只提供资源和只读配置；动效层拆为资源配置、纯引擎和 React 生命周期外壳，不修改 `AppShell`、路由、首页、模板、结果、历史或 PRD。后续通过单独集成提交把组件挂到主页。

**Tech Stack:** React 19、TypeScript 5.9、Vite 7、Canvas 2D、Vitest 3、Testing Library、CSS。

## Global Constraints

- 基准提交固定为 `ee7f523`，不得把主 worktree 的未提交文件带入本分支。
- 不修改 Trae Task 8—10 声明的任何文件，包括结果、下载、历史、`DashboardPage.tsx`、`TemplateCard.tsx`、`AppShell.test.tsx`、README、PRD和项目介绍。
- 不接入 `AppRouter.tsx`、`AppShell.tsx`、`pages.css`、`tokens.css` 或任何现有页面。
- 官方 Logo 必须保留 SVG，不改色、不加底板、不转 PNG，项目内目标为 `frontend/src/assets/brand/qianjing-zhizuo-logo-mark.svg`。
- 粒子背景只提供组件能力，集成后仅允许主页挂载；Canvas 必须 `aria-hidden="true"` 且 `pointer-events:none`。
- 产品运行时不得依赖微信容器路径、Trae 附件绝对路径、`particle-reveal-demo.html` 或 base64 中间文件。
- 桌面配置：`CELL=7`、`DOT_R=2.4`、DPR 上限 2、最多 60fps。
- 移动配置：`CELL=9`、DPR 上限 1.5、最多 30fps。
- `prefers-reduced-motion: reduce`、页面隐藏或图片加载失败时，前景生成流程必须仍可用。
- 粒子插画红保留 `#D4351C`；产品交互主色 `#E60012` 不进入粒子调色板。
- 每个实现任务完成后运行指定测试并使用中文 Commit Message 提交。

---

## File Map

### Brand

- `frontend/src/assets/brand/qianjing-zhizuo-logo-mark.svg`：官方红黑矢量图形标。
- `frontend/src/config/brand.ts`：中文名、英文名、缩写和 Logo URL 的唯一配置入口。
- `frontend/src/config/brand.test.ts`：锁定品牌文字和项目内资源引用，防止绝对路径回流。
- `frontend/src/vite-env.d.ts`：Vite 静态资源类型声明，供 SVG 和粒子图片导入复用。

### Particle background

- `frontend/src/assets/motion/particle-reveal/reveal-01.png` 至 `reveal-10.png`：项目内 10 张显影图。
- `frontend/src/features/background/particleRevealAssets.ts`：资源 URL、五色调色板、桌面/移动性能配置。
- `frontend/src/features/background/particleRevealEngine.ts`：纯 Canvas 引擎，负责加载、网格、粒子、显影、暂停、恢复、Resize 与销毁。
- `frontend/src/features/background/particleRevealEngine.test.ts`：验证确定性时序、配置选择、RAF 生命周期和资源失败回退。
- `frontend/src/features/background/ParticleRevealBackground.tsx`：React 生命周期外壳，负责媒体查询、页面可见性、ResizeObserver 与清理。
- `frontend/src/features/background/ParticleRevealBackground.test.tsx`：验证无障碍、减少动态效果、卸载清理和可见性暂停。
- `frontend/src/styles/particle-reveal.css`：组件局部表面、Canvas 和可读性遮罩样式；本任务不导入全局样式入口。

---

### Task 1: 建立品牌资源与只读配置

**Files:**
- Create: `frontend/src/assets/brand/qianjing-zhizuo-logo-mark.svg`
- Create: `frontend/src/config/brand.ts`
- Create: `frontend/src/config/brand.test.ts`
- Create: `frontend/src/vite-env.d.ts`

**Interfaces:**
- Produces: `BRAND = {nameZh:'黔景智作', nameEn:'QianScape AI', shortName:'QSAI', logoUrl:string} as const`

- [ ] **Step 1: 先写品牌配置失败测试**

```ts
import {describe,expect,it} from 'vitest';
import {BRAND} from './brand';

describe('BRAND',()=>{
  it('使用已确认名称和项目内 SVG',()=>{
    expect(BRAND).toMatchObject({
      nameZh:'黔景智作',
      nameEn:'QianScape AI',
      shortName:'QSAI',
    });
    expect(BRAND.logoUrl).toMatch(/^\/(?:frontend\/)?src\/assets\/brand\/.+\.svg\?no-inline$/);
    expect(BRAND.logoUrl).not.toMatch(/Users|xwechat|file:\/\//);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
npx vitest run frontend/src/config/brand.test.ts --config frontend/vitest.config.ts
```

Expected: FAIL，因为 `frontend/src/config/brand.ts` 尚不存在。

- [ ] **Step 3: 复制官方 SVG 并实现配置**

复制来源：

```text
/Users/azu/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_yyvmltnuz4hp22_e9d0/msg/file/2026-08/11-qianjing-zhizuo-logo-mark.svg
```

`frontend/src/config/brand.ts`：

```ts
import logoUrl from '../assets/brand/qianjing-zhizuo-logo-mark.svg?no-inline';

export const BRAND={
  nameZh:'黔景智作',
  nameEn:'QianScape AI',
  shortName:'QSAI',
  logoUrl,
} as const;
```

- [ ] **Step 4: 验证 SVG 与测试**

Run:

```bash
file frontend/src/assets/brand/qianjing-zhizuo-logo-mark.svg
rg -n "#E60012|#000000|<title|<desc" frontend/src/assets/brand/qianjing-zhizuo-logo-mark.svg
npx vitest run frontend/src/config/brand.test.ts --config frontend/vitest.config.ts
npm run typecheck
```

Expected: SVG 可识别，包含可访问标题/描述与红黑标准色；测试和 typecheck PASS。

- [ ] **Step 5: 提交品牌基础**

```bash
git add frontend/src/assets/brand frontend/src/config/brand.ts frontend/src/config/brand.test.ts frontend/src/vite-env.d.ts
git commit -m "品牌：加入黔景智作官方标识与配置"
```

---

### Task 2: 建立粒子资源配置与可销毁引擎

**Files:**
- Create: `frontend/src/assets/motion/particle-reveal/reveal-01.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-02.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-03.jpg`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-04.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-05.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-06.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-07.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-08.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-09.png`
- Create: `frontend/src/assets/motion/particle-reveal/reveal-10.png`
- Create: `frontend/src/features/background/particleRevealAssets.ts`
- Create: `frontend/src/features/background/particleRevealEngine.ts`
- Create: `frontend/src/features/background/particleRevealEngine.test.ts`

**Interfaces:**
- Produces: `PARTICLE_COLORS: readonly [number,number,number][]`
- Produces: `PARTICLE_PROFILES: {desktop:ParticleProfile; mobile:ParticleProfile}`
- Produces: `createSeededRandom(seed:number): ()=>number`
- Produces: `getRevealStrength(timing:RevealTiming,time:number): number`
- Produces: `createParticleRevealEngine(options:ParticleRevealEngineOptions): ParticleRevealEngine`
- Produces: `ParticleRevealEngine = {ready:Promise<void>; start():void; pause():void; resize(width:number,height:number,dpr:number):void; destroy():void}`
- Produces: `ParticleRevealEngineOptions = {canvas:HTMLCanvasElement; imageUrls:readonly string[]; profile:ParticleProfile; seed:number; context?:CanvasRenderingContext2D; scheduler?:AnimationScheduler; imageLoader?:ImageLoader}`

- [ ] **Step 1: 复制 10 张原图为稳定项目资源**

按 `generate_demo.py` 的既定顺序复制并重命名为 `reveal-01` 至 `reveal-10`；第三张保留 `.jpg`，其余保留 `.png`。完成后运行：

```bash
find frontend/src/assets/motion/particle-reveal -maxdepth 1 -type f -print | sort
du -ch frontend/src/assets/motion/particle-reveal/* | tail -1
```

Expected: 精确 10 个文件，总大小小于 1MB，不存在 base64 文本。

- [ ] **Step 2: 写配置与生命周期失败测试**

测试至少覆盖：

```ts
expect(PARTICLE_PROFILES.desktop).toMatchObject({cell:7,dotRadius:2.4,maxDpr:2,maxFps:60});
expect(PARTICLE_PROFILES.mobile).toMatchObject({cell:9,dotRadius:2.4,maxDpr:1.5,maxFps:30});
expect(PARTICLE_COLORS[0]).toEqual([212,53,28]);

const engine=createParticleRevealEngine({canvas,imageUrls:['/one.png'],profile:PARTICLE_PROFILES.desktop,seed:42,scheduler,imageLoader});
engine.start();
expect(scheduler.request).toHaveBeenCalledTimes(1);
engine.pause();
expect(scheduler.cancel).toHaveBeenCalledTimes(1);
engine.destroy();
engine.destroy();
expect(scheduler.cancel).toHaveBeenCalledTimes(1);
```

另测：相同 seed 得到相同放置序列；图片加载失败不会抛出未处理异常；`resize` 把 DPR 钳制到 profile 的 `maxDpr`。

- [ ] **Step 3: 运行测试确认失败**

Run:

```bash
npx vitest run frontend/src/features/background/particleRevealEngine.test.ts --config frontend/vitest.config.ts
```

Expected: FAIL，因为配置和引擎尚不存在。

- [ ] **Step 4: 实现资源配置**

`particleRevealAssets.ts` 使用静态 import 导出 10 个 Vite URL，并定义：

```ts
export type ParticleProfile={
  cell:number;
  dotRadius:number;
  maxDpr:number;
  maxFps:number;
  baseFade:number;
};

export const PARTICLE_PROFILES={
  desktop:{cell:7,dotRadius:2.4,maxDpr:2,maxFps:60,baseFade:0.018},
  mobile:{cell:9,dotRadius:2.4,maxDpr:1.5,maxFps:30,baseFade:0.018},
} as const satisfies Record<'desktop'|'mobile',ParticleProfile>;
```

- [ ] **Step 5: 从 Demo 提取纯引擎**

引擎保留 `handoff-doc.md` 已确认的算法：固定水平行粒子、网格墨迹衰减、五色最近色分类、10 图随机无重叠放置、`FLOW → RIN → HOLD → ROUT`、3+3+3+1 错峰。禁止复制 Demo 的 `<body>`、标题、全局样式和 base64 数组。

引擎内部必须满足：

- `start()` 幂等，最多存在一个 RAF。
- `pause()` 取消 RAF 但保留同一挂载周期的随机种子和网格。
- `resize()` 使用传入尺寸重建画布；200ms 防抖由 React 外壳负责。
- `destroy()` 幂等，取消 RAF、清空数组和 Canvas，之后 `start()` 不再生效。
- 图片加载全部失败时只绘制浅色背景和粒子，不向外抛异常。

- [ ] **Step 6: 运行引擎测试与静态检查**

Run:

```bash
npx vitest run frontend/src/features/background/particleRevealEngine.test.ts --config frontend/vitest.config.ts
npm run lint
npm run typecheck
```

Expected: 全部 PASS。

- [ ] **Step 7: 提交粒子引擎**

```bash
git add frontend/src/assets/motion frontend/src/features/background/particleRevealAssets.ts frontend/src/features/background/particleRevealEngine.ts frontend/src/features/background/particleRevealEngine.test.ts
git commit -m "动效：实现可销毁的主页粒子显影引擎"
```

---

### Task 3: 建立 React 生命周期外壳与局部样式

**Files:**
- Create: `frontend/src/features/background/ParticleRevealBackground.tsx`
- Create: `frontend/src/features/background/ParticleRevealBackground.test.tsx`
- Create: `frontend/src/styles/particle-reveal.css`

**Interfaces:**
- Consumes: `createParticleRevealEngine`、`PARTICLE_IMAGE_URLS`、`PARTICLE_PROFILES`
- Produces: `ParticleRevealBackground({className?:string; seed?:number}): JSX.Element`
- Produces: CSS classes `particle-reveal-background`、`particle-reveal-background__canvas`、`particle-reveal-background__veil`

- [ ] **Step 1: 写 React 生命周期失败测试**

测试 mock `createParticleRevealEngine` 并覆盖：

```ts
expect(screen.getByTestId('particle-reveal-canvas')).toHaveAttribute('aria-hidden','true');
expect(engine.start).toHaveBeenCalledTimes(1);

document.dispatchEvent(new Event('visibilitychange'));
expect(engine.pause).toHaveBeenCalled();

unmount();
expect(engine.destroy).toHaveBeenCalledTimes(1);
```

另测：`prefers-reduced-motion: reduce` 时不调用 `start()`；宽度小于 768px 使用 mobile profile；ResizeObserver 连续触发时只在 200ms 后调用一次 `resize()`。

- [ ] **Step 2: 运行测试确认失败**

Run:

```bash
npx vitest run frontend/src/features/background/ParticleRevealBackground.test.tsx --config frontend/vitest.config.ts
```

Expected: FAIL，因为组件尚不存在。

- [ ] **Step 3: 实现 React 外壳**

组件渲染固定三层：

```tsx
<div className={classes} data-testid="particle-reveal-background">
  <canvas aria-hidden="true" className="particle-reveal-background__canvas" data-testid="particle-reveal-canvas" />
  <div aria-hidden="true" className="particle-reveal-background__veil" />
</div>
```

挂载时选择 profile、创建引擎并在非 reduce 模式启动；监听 `visibilitychange`；ResizeObserver 使用 200ms 定时器合并；卸载时依次清理定时器、Observer、监听器并调用一次 `destroy()`。

- [ ] **Step 4: 实现不污染全局的局部样式**

```css
.particle-reveal-background{position:fixed;inset:0;z-index:0;overflow:hidden;pointer-events:none;background:#fafafa}
.particle-reveal-background__canvas{width:100%;height:100%;display:block;opacity:.78}
.particle-reveal-background__veil{position:absolute;inset:0;background:radial-gradient(ellipse 64% 50% at 50% 40%,rgb(250 250 250 / 72%) 0%,rgb(250 250 250 / 46%) 46%,rgb(250 250 250 / 0%) 78%)}
@media (prefers-reduced-motion:reduce){.particle-reveal-background__canvas{display:none}}
```

不得写 `html`、`body`、`#root` 或通用元素选择器。本任务不把该 CSS 加入 `pages.css`，避免改变 Trae 当前页面。

- [ ] **Step 5: 运行专项与全量验证**

Run:

```bash
npx vitest run frontend/src/config/brand.test.ts frontend/src/features/background --config frontend/vitest.config.ts
npm run lint
npm run typecheck
npm test
npm run build
git diff --check
```

Expected: 全部 PASS；构建产物包含 10 张资源文件；没有现有页面视觉变化，因为组件和 CSS 尚未接线。

- [ ] **Step 6: 提交 React 外壳**

```bash
git add frontend/src/features/background/ParticleRevealBackground.tsx frontend/src/features/background/ParticleRevealBackground.test.tsx frontend/src/styles/particle-reveal.css
git commit -m "动效：完成主页粒子背景组件与生命周期"
```

---

## Merge Contract

Trae 完成当前提交后，只合并本分支的三个实现提交，不合并主 worktree 未提交内容。推荐先在目标分支执行：

```bash
git status --short
git cherry-pick <品牌提交> <引擎提交> <组件提交>
npm run lint
npm run typecheck
npm test
npm run build
```

本分支刻意不完成主页接线。后续集成只需在新的首页组件中导入 `ParticleRevealBackground` 和 `particle-reveal.css`，并在非主页不渲染该组件。

## Self-Review

- 设计覆盖：品牌名称、官方 SVG、Canvas 2D、10 图、桌面/移动配置、主页专用生命周期、可读性遮罩、减少动态效果和失败回退均有任务承接。
- 冲突覆盖：没有修改 Trae Task 8—10 文件，也没有修改全局样式入口或现有页面。
- 类型一致：资源、profile、engine 和 React props 的命名在三个任务中一致。
- 占位符检查：无 TBD、TODO、“稍后实现”或未定义接口。
