# 黔景智作首页与导航体验设计

> 日期：2026-08-29
> 文档类型：UX/UI Design Specification
> 状态：已实施并完成自动化与浏览器结构验收
> 项目路径：`/Users/azu/Documents/guikesong`
> 品牌：黔景智作 / QianScape AI / QSAI

## 1. 文档定位

本文档定义黔景智作新一版首页、顶部导航、模板入口和一句话生成入口的最终 UX/UI 方向。它同时包含：

- UX：页面层级、页面职责、跳转、选择和生成流程。
- UI：毛玻璃材质、透明度、排版、宽度、间距、圆角、阴影、组件状态和响应式规则。
- 动效接入边界：为用户另行制作的页面背景动效预留稳定承载层，不定义动效具体画面。

本文档是后续界面改造的最新设计依据。它取代旧规范中与以下内容冲突的部分：固定侧边导航、任务优先工作台、独立模板创建页和首页最近任务区。结果详情、本机历史、下载、部分成功和参考图业务规则继续沿用既有 PRD。

本设计已按两条正式工作流实施；主 PRD、原 UX/UI 规范、README 和项目介绍同步完成。结果与历史业务合同未改变。

## 2. 设计目标

用户打开产品后，不需要先理解工作台、表单或模型参数，直接完成三件事：

1. 默认使用最常用模板。
2. 输入一句话，可选添加参考图。
3. 按 Enter 或点击生成，进入独立结果页。

整体气质采用“产品化电子杂志”：页面有清晰的编辑感和文旅气质，但保持产品可用性。未来动态背景负责氛围，导航和生成输入区用克制的毛玻璃材质建立可读层级。

## 3. 品牌使用

| 类型 | 最终名称 | 主要位置 |
|---|---|---|
| 中文品牌名 | 黔景智作 | 桌面顶部导航左侧、页面标题和正式文档 |
| 英文品牌名 | QianScape AI | 中文名下方或品牌锁定组合中 |
| 品牌缩写 | QSAI | 品牌图形、窄屏导航和紧凑场景 |

规则：

- 桌面端优先显示 QSAI 标记、黔景智作和小号 QianScape AI。
- 手机端空间不足时，只保留 QSAI 标记；不得压缩中部导航或历史按钮。
- 品牌区是返回首页入口。
- 品牌名与模板名均来自集中配置，避免散落在组件中。

### 3.1 官方图形标

用户已提供官方独立图形标 `11-qianjing-zhizuo-logo-mark.svg`：

- 原始画布：640×640，透明背景，矢量格式。
- 标准色：品牌红 `#E60012` 与黑色 `#000000`。
- 核心构成：Q 形文化景窗、贵州喀斯特峰林、梯田与内容流、苗族纹样、AI 星芒与数据节点。
- 原文件包含可访问的 `title` 和 `desc`，执行时应保留。

执行阶段必须把用户提供的 SVG 复制到项目品牌资源目录，建议目标为：

```text
frontend/src/assets/brand/qianjing-zhizuo-logo-mark.svg
```

不得在运行时代码中引用微信容器原始路径，也不得把 SVG 转成低分辨率 PNG 作为页面主资源。

导航使用规则：

- 桌面端图形标显示尺寸 40×40px；移动端 36×36px。
- 图形标与文字锁定组合间距 10px。
- 不增加圆形、方形或渐变底板，直接显示在毛玻璃导航上。
- 不改变红黑配色，不增加阴影、描边、旋转或发光。
- 桌面文字锁定组合中，中文名使用 15px/20px、字重 600；英文名使用 10px/14px、字重 500。
- 当空间不足以容纳文字锁定组合时，只隐藏文字，不压缩或拉伸图形标。

## 4. 信息架构

```text
黔景智作
├── 主页 `/`
│   ├── 品牌式标题
│   ├── 一句话生成输入框
│   └── 常用模板横向选择
├── 全部模板 `/templates`
│   └── 模板列表
├── 模板详情 `/templates/:templateId`
│   ├── 模板预览
│   ├── 适用场景
│   ├── 输入建议
│   ├── 示例
│   └── 用此模板创作
├── 原创 IP 首次配置 `/templates/original-ip/create`（仅无锁定档案时）
├── 生成结果 `/results/:requestId`
├── 历史记录 `/history`
└── 历史详情 `/history/:recordId`
```

页面职责：

- 主页承担完整生成入口，不展示最近任务、数据统计或工作台卡片。
- 全部模板负责浏览和比较模板，不承担生成表单。
- 模板详情负责解释单个模板，不重复放置完整生成表单。
- 结果详情负责查看、复制和下载。
- 历史记录只从顶部右侧历史图标进入。
- `/templates/:templateId/create` 不再是日常主路径；小红书图鉴完全从主页生成。原创 IP 已锁定档案时也从主页生成，尚无锁定档案时保留文字和产品图进入必要配置页。

## 5. 页面总体结构

页面从后向前分为四层：

1. `BackgroundMotionLayer`：未来动态背景，铺满视口。
2. 页面内容层：首页标题和模板内容。
3. 毛玻璃表面层：顶部导航、当前页签、生成输入框。
4. 浮层：菜单、提示、沉浸预览和对话框。

桌面端宽度：

- 顶部导航：`100vw`，从左到右铺满，不设外层圆角。
- 首页主内容：`min(90vw, 1440px)`。
- 标题和生成输入区：`min(76vw, 1040px)`。
- 常用模板轨道：使用首页主内容全宽，不与标题列一起缩成窄栏。
- 页面左右最小安全边距：32px；1600px 以上不继续无限放大内容。

纵向节奏：

- 顶部导航高度：72px。
- 首页主内容顶部留白：`clamp(56px, 7vh, 88px)`，保持标题和输入区位于首屏上半部。
- 眉题到主标题：12px。
- 主标题两行内部行距：1.12 倍。
- 主标题到生成输入框：桌面使用 `clamp(44px, 6vh, 64px)`，让生成框更接近首屏纵向中心；手机为 32px。不再重复显示与占位符相同的辅助说明。
- 生成输入区和常用模板区之间使用弹性留白，常用模板贴近首屏底部。
- 常用模板标题到卡片轨道：8px。

目标是形成舒展的横向画布，而不是把所有内容挤在页面中心的小卡片中。

## 6. 顶部导航

### 6.1 布局

顶部导航固定在视口上方，分为三段：

| 位置 | 内容 | 对齐规则 |
|---|---|---|
| 左侧 | QSAI + 黔景智作 + QianScape AI | 左对齐，品牌区不参与中部居中计算 |
| 中部 | 主页 / 全部模板 | 以视口绝对水平中心定位 |
| 右侧 | 历史记录图标 | 右对齐，纯图标按钮 |

中部导航必须相对整个视口居中，不能因为左右内容宽度不同产生偏移。

### 6.2 导航毛玻璃

顶部导航最终背景透明度为 60%。推荐实现 Token：

```css
--glass-nav-bg: rgb(246 248 247 / 60%);
--glass-nav-blur: 20px;
--glass-nav-saturation: 140%;
--glass-nav-border: rgb(255 255 255 / 42%);
--glass-nav-shadow: 0 1px 0 rgb(20 20 20 / 8%);
```

表现规则：

- 导航从左到右全宽铺开，不使用外层圆角。
- 只保留底部 1px 柔和分隔，不使用悬浮大阴影。
- 透明度只作用于背景层；品牌、文字和图标保持 100% 不透明。
- `backdrop-filter` 不可用时，退化为不透明度更高的浅雾灰背景，仍保证文字可读。

### 6.3 中部活动页签

“主页 / 全部模板”使用接入导航底边的活动页签，而不是独立悬浮胶囊：

- 当前页签顶部为圆角椭圆轮廓。
- 页签底边与导航最底部严格平齐，不能向页面内容区延伸。
- 页签左右使用短而平滑的连接翼过渡到导航底边。
- 非当前项保持透明，只显示文字和轻量悬停反馈。
- 当前项使用带透明度的雾灰，不使用纯白实心块。

最终 Token：

```css
--glass-tab-active-bg: rgb(232 236 234 / 78%);
--glass-tab-active-border: rgb(255 255 255 / 48%);
--glass-tab-active-radius: 999px 999px 0 0;
```

活动页签高度 44px，顶部圆角采用全圆角语义，底部圆角为 0。页签文字 14px/20px、字重 600；当前项使用主文字色，非当前项使用辅助文字色。

连接翼的几何约束：

- 当前项主体宽度以文字宽度加左右各 24px 内边距计算。
- 左右连接翼各向外延伸 20px，高度 12px，只存在于导航内部的底边区域。
- 连接翼与主体使用同一雾灰背景，通过四分之一圆曲线接入导航底边。
- 主体、连接翼和导航底边的最低点共用同一条 72px 基线；任何伪元素都不得超出导航边界。

### 6.4 历史入口

- 右侧只显示一个历史图标，不放“历史记录”常驻文字。
- 点击区域不小于 44×44px，图标 20px，线宽统一。
- 必须提供 `aria-label="历史记录"` 和悬停提示。
- 当前处于历史页时，图标显示轻量选中态，但不占用中部活动页签。

## 7. 首页文字排版

首页创作区采用主标题与输入框下说明两个层级；删除旧英文眉题和旧中文标题，输入提示只保留在输入框内：

```text
QIANSCAPE AI · TRAVEL CREATIVE STUDIO

你给一个选题
它还你一座城的流量

输入框下方：一次灵感输入，全套图文输出，文旅爆款即刻启程。
```

排版层级：

| 角色 | 字体建议 | 桌面字号/行高 | 字重 | 说明 |
|---|---|---:|---:|---|
| 中文主标题 | 现代中文衬线字体栈 | `clamp(32px, 3vw, 48px)` / 1.12 | 500 | 两行，克制显示，不压迫输入区 |
| 输入框下说明 | 中文无衬线字体栈 | 12px / 18px | 400 | 置于输入框下方，使用辅助文字色 |
| 产品正文 | 中文无衬线字体栈 | 14px / 22px | 400 | 表单、详情和反馈 |

字体栈原则：

- 标题优先使用系统可用的中文宋体/现代衬线字体，缺失时稳定回退。
- 正文和控件使用无衬线字体，保证长时间阅读和交互清晰。
- 元数据使用等宽字体，只用于英文眉题、编号和短标签。
- 不在标题旁堆叠图标、标签和解释性按钮。
- 首版不依赖在线字体服务，建议字体栈如下：

```css
--font-display: "Songti SC", "Noto Serif CJK SC", "Source Han Serif SC", serif;
--font-sans: "PingFang SC", "Microsoft YaHei", "Noto Sans CJK SC", system-ui, sans-serif;
--font-mono: "SFMono-Regular", Menlo, Consolas, monospace;
```

## 8. 一句话生成输入框

### 8.1 结构

生成输入框是首页唯一主交互，内部只保留：

- 多行文字输入。
- `＋` 添加图片按钮。
- `↑` 生成按钮。

不显示模型选择、参数设置、提示词配置或额外主按钮。

默认结构采用两列网格：

```text
┌──────────────────────────────────────────────────────────┐
│ 输入一句话需求……                         ＋   ↑          │
└──────────────────────────────────────────────────────────┘
```

最终尺寸：

| 参数 | 数值 |
|---|---:|
| 默认最小高度 | 60px |
| 横向内边距 | 左 16px / 右 10px |
| 纵向内边距 | 8px |
| 文字区与右侧按钮组间距 | 20px |
| 按钮组内部间距 | 8px |
| 图标按钮视觉尺寸 | 36×36px |
| 图标按钮可点击区域 | 不小于 44×44px |
| 文本字号/行高 | 15px / 24px |
| 最大自动增长高度 | 168px |

输入文字和右侧按钮组最终使用 20px 间距。布局禁止使用 `justify-content: space-between` 制造不可控的大空白；文字列用 `minmax(0, 1fr)`，按钮组保持内容宽度。

### 8.2 输入框毛玻璃

输入框背景最终不透明度为 90%，比顶部导航更实，以保证长文本可读：

```css
--glass-composer-bg: rgb(250 251 250 / 90%);
--glass-composer-blur: 16px;
--glass-composer-border: rgb(255 255 255 / 62%);
--glass-composer-shadow:
  0 1px 2px rgb(20 20 20 / 5%),
  0 14px 36px rgb(20 20 20 / 10%);
--glass-composer-radius: 18px;
```

规则：

- 90% 只作用于输入框背景；输入文字、占位符、图标和焦点环不透明。
- 恢复中央径向遮罩：桌面中心最高 72%、中段 42% 并在 82% 位置归零；手机中心最高 76%，边缘完整保留粒子。遮罩不使用矩形边界或输入框局部白块。
- Canvas 本身在桌面使用 62% 视觉强度、手机使用 52%，由径向渐变保护标题和输入区可读性。
- 默认使用细边界和低阴影；文本输入聚焦只使用中性边界与轻阴影，不显示品牌红色外框。加图、生成等独立按钮仍保留可见键盘焦点。
- 输入框圆角 18px，只用于这一主交互；普通业务卡片不得跟随放大圆角。
- 输入变长时向下增长，右侧按钮组贴近输入区域的垂直中线或底部，不漂到远处。

### 8.3 图片附件

- 点击 `＋` 选择图片，继续沿用最多 4 张、JPG/PNG/WebP、单张不超过 10MB 的业务限制。
- 已选图片以 44×44px 紧凑缩略图显示在输入框内部下方，不另起大型上传卡片。
- 单张图片提供移除按钮和明确上传/失败状态。
- 图片区出现后，文字区与图片区间距 12px；按钮组仍位于右侧，不与缩略图混排。

### 8.4 键盘和提交

- `Enter`：直接开始生成。
- `Shift + Enter`：换行。
- 中文输入法正在组词时，`Enter` 只确认候选字，不得误提交。
- 点击 `↑` 与按 Enter 调用同一提交逻辑。
- 输入去除首尾空白后少于 2 个字符时，生成按钮禁用并显示就地说明。
- 生成中禁止重复提交，箭头切换为紧凑加载状态并显示“正在生成”。
- 生成成功或部分成功后进入 `/results/:requestId`。
- 失败时保留文字、模板和仍有效的参考图，在输入框下方显示可恢复错误。

## 9. 常用模板

### 9.1 最终排序

常用模板按以下顺序固定展示：

1. 小红书图鉴创作（`xhs-atlas`）
2. 原创 IP 商品化（`original-ip`）

首页首次进入时默认选中第一项“小红书图鉴创作”。用户无需额外点击模板，输入含数量的一句话即可生成；原创 IP 按用户要求后置。

模板展示名未来允许调整，但排序、选择和历史兼容必须依赖稳定模板 ID，不得依赖中文名称。

### 9.2 卡片结构

每张模板卡片采用“名称在上、图片在下”：

```text
小红书图鉴创作                     详情
┌────────────────────────────────────┐
│                                    │
│             模板预览图              │
│                                    │
└────────────────────────────────────┘
```

- 顶部名称 13px/20px、字重 600。
- “详情”紧跟名称右侧，使用 11px/16px、字重 500 和辅助文字色；可见但不抢主视觉。
- 图片比例由模板配置提供，卡片使用统一预览容器避免跳动。
- 卡片不放长描述、功能标签或大号操作按钮。
- 选中态使用 1px 强调边界和小型勾选标识，不使用整卡高饱和填充。

### 9.3 横向轨道

- 桌面端单卡宽度 220—270px，间距 16px；模板只作底部快速选择，不与生成输入争夺视觉重心。
- 轨道使用横向滚动和 `scroll-snap`，支持触控板、鼠标滚轮横向转换和触摸滑动。
- 有溢出时显示克制的左右翻页按钮；没有溢出时隐藏。
- 轨道两端不使用遮挡卡片文字的重渐变。
- 手机端允许看到下一张卡片的一部分，提示用户可以横向滑动。

### 9.4 点击规则

- 点击图片或模板名称：只切换当前模板，留在主页。
- 点击“详情”：进入 `/templates/:templateId`。
- 切换模板不清空已输入文字和已选参考图。
- 当前模板 ID 与生成请求保持一致。

## 10. 全部模板与模板详情

### 10.1 全部模板

顶部中部“全部模板”处于活动页签状态。每张模板提供两个层级明确的操作：

- 使用模板：回到主页，自动选中该模板并聚焦生成输入框。
- 查看详情：进入该模板的独立详情页。

全部模板页可容纳后续新增工作流入口，但本轮不增加搜索、筛选、收藏、推荐或模板市场。

桌面端模板列表最大宽度收敛为 920px，两张卡片各约 440px，视觉面积约为初版的一半；预览使用 16:9，卡片内容内边距 16px。全部模板页用于快速比较，不使用接近详情页尺寸的大幅卡片。

### 10.2 模板详情

模板详情至少包含：

- 大幅预览图。
- 模板用途和适用场景。
- 一句话输入建议。
- 示例输入和预期输出说明。
- “用此模板创作”主操作。

点击“用此模板创作”后返回主页、选中当前模板并聚焦生成输入框。详情页不重复上传区和生成表单。

模板详情属于“全部模板”体系：进入详情后，中部“全部模板”仍保持活动归属，用户可以从浏览器返回或页面返回入口回到模板列表。

## 11. 结果与历史

- 新生成和部分成功结果进入独立结果详情，不向主页下方追加结果。
- 结果详情继续复用已有缩略图、主图、文案、复制、下载和沉浸预览能力。
- 成功或部分成功结果继续写入本机 IndexedDB。
- 历史列表和历史详情继续保留单条删除、清空、复制和下载能力。
- 首页不展示最近历史；历史统一从顶部右侧图标进入。
- 返回首页时保留默认或最近明确选择的模板；不自动把历史模板当作新的默认排序。

## 12. 视觉 Token

### 12.1 色彩

```css
--color-brand-red-700: #b6000e;
--color-brand-red-600: #e60012;
--color-brand-red-100: #ffe3e5;
--color-ink-900: #141414;
--color-ink-700: #363636;
--color-ink-500: #6a6a6a;
--color-secondary-teal: #1f6761;
--color-border-soft: rgb(0 0 0 / 12%);
--color-focus: rgb(230 0 18 / 32%);
```

品牌红 `#E60012` 是页面唯一主操作色：用于生成按钮、模板选中边界、小型勾选和键盘焦点。悬停或按下使用 `#B6000E`，浅选中背景使用 `#FFE3E5`。白色文字位于品牌红按钮上时必须保持清晰对比。

青绿色 `#1F6761` 降为辅助色，只允许用于少量文旅内容提示或非主操作信息，不用于生成按钮、当前模板、导航活动页签或主要焦点。顶部活动页签继续使用已确认的透明雾灰。

未来背景动效可以提供文旅色彩，但前景控件不增加紫色霓虹、外发光或其他竞争主色。

### 12.2 间距

沿用 4px 网格：4、8、12、16、20、24、32、40、44、48、64px。新增的 44px 仅用于本设计中输入框到模板区的页面节奏和最小点击区域，不作为通用组件内边距。

同一信息组优先 8—12px；同一组件的主要区域优先 16—20px；页面区块优先 32—48px。

### 12.3 圆角

| Token | 数值 | 用途 |
|---|---:|---|
| `--radius-sm` | 6px | 小标签、缩略图内部元素 |
| `--radius-md` | 10px | 普通按钮、小型控件 |
| `--radius-lg` | 14px | 模板预览和普通内容容器 |
| `--radius-composer` | 18px | 首页生成输入框 |
| `--radius-full` | 999px | 活动页签顶部、圆形图标按钮 |

顶部导航外层圆角固定为 0；活动页签底部圆角固定为 0。

### 12.4 阴影

```css
--shadow-low: 0 1px 2px rgb(20 20 20 / 6%);
--shadow-composer: 0 14px 36px rgb(20 20 20 / 10%);
--shadow-float: 0 10px 28px rgb(20 20 20 / 14%);
--shadow-modal: 0 22px 56px rgb(20 20 20 / 18%);
```

- 模板卡片默认无大阴影，悬停最多使用 `shadow-low`。
- 生成输入框允许使用 `shadow-composer`，表达首页唯一主交互。
- 菜单和提示浮层使用 `shadow-float`。
- 模态和沉浸预览使用 `shadow-modal`。
- 阴影统一使用透明中性黑，不使用实心纯黑或带品牌红的彩色阴影。

## 13. 状态与反馈

### 13.1 模板状态

- 默认：中性文字和边界。
- 悬停：边界加深，图片轻微提亮。
- 选中：品牌红边界、小勾选、名称字重提升。
- 聚焦：清晰 2px 外焦点环。
- 禁用：只在模板确实不可用时出现，并说明原因。

### 13.2 生成状态

- 空输入：生成按钮禁用。
- 可生成：箭头按钮使用品牌红，`＋` 按钮保持中性。
- 生成中：阻止重复提交，显示真实“正在生成”，不伪造百分比。
- 部分成功：仍进入结果页，由结果页展示成功与失败页面。
- 失败：留在主页，保留输入和附件，提供明确重试。

### 13.3 页面切换

- 活动页签切换使用 180—220ms，缓动 `cubic-bezier(0.16, 1, 0.3, 1)`。
- 模板选中切换使用 140—180ms。
- 不做整页入场表演、滚动劫持或无限漂浮控件。
- 所有交互动效支持 `prefers-reduced-motion`。

## 14. 背景动效接入合同

### 14.1 最终范围

背景动效最终只用于主页 `/`：

- 进入主页时挂载并启动动画。
- 离开主页进入全部模板、模板详情、结果、历史或历史详情时，必须卸载动画并停止渲染循环。
- 非主页统一使用静态浅色背景 `#FAFAFA`，不保留低速粒子或持续显影。
- 顶部导航在所有页面保持同一结构；非主页下虽然没有动态内容，仍使用同一雾灰材质和边界，避免页面切换时导航尺寸变化。

### 14.2 Trae 交付基准

现有交付文件位于项目根目录：

| 文件 | 定位 |
|---|---|
| `particle-reveal-demo.html` | 最终视觉基准，Canvas 2D 单文件 Demo，约 1.1MB |
| `generate_demo.py` | 将 10 张原图转为 base64 并重新生成 Demo 的构建脚本 |
| `handoff-doc.md` | 粒子、网格、显影时序、颜色和性能说明 |
| `analyze_image.py` | 原图颜色分析辅助脚本，不进入产品运行链路 |
| `image_data_url.txt` | 单图 base64 中间产物，不进入产品运行链路 |

视觉基准参数：

- Canvas 2D，浅灰白底。
- `CELL=7px`，`DOT_R=2.4px`。
- 粒子沿固定水平行从左向右流动。
- 红、黑、棕、黄、绿五色粒子持续留下并衰减墨迹。
- 10 张民族风插画按 3+3+3+1 错峰显影。
- 单张经历流动、显入、停留、显出四个阶段。
- 同一时间窗口最多 3 张进入显影过程。

`particle-reveal-demo.html` 作为视觉比对基准保留，但产品不得通过 iframe 直接嵌入整份 HTML。

### 14.3 产品接入结构

推荐把 Demo 拆为三个边界清晰的前端模块：

```text
ParticleRevealBackground.tsx
├── 管理挂载、卸载、可见性、ResizeObserver 和无障碍属性
particleRevealEngine.ts
├── 管理网格、粒子、显影区域、动画帧和资源释放
particleRevealAssets.ts
└── 管理 10 张图片、五色配置和桌面/移动参数
```

接入规则：

- 原始 10 张图片必须复制到项目内稳定资源目录，不能依赖 `/Users/azu/.trae-cn/attachments/...` 绝对路径。
- 产品运行时使用项目资源或构建产物，不继续把所有图片作为超长 base64 字符串写进 React 组件。
- 删除 Demo 左上角说明文字。
- 禁止把 Demo 的 `body { overflow: hidden; }` 等全局样式带入产品。
- Canvas 使用 `aria-hidden="true"`，固定铺满主页视口，并设置 `pointer-events: none`。
- 动效不得改变页面结构尺寸，不能造成布局跳动或阻断模板横向滚动。
- 首版保留 Canvas 2D，不在本轮改写为 WebGL、OffscreenCanvas 或 Worker。

推荐前景分层顺序：

```text
z-index 0   ParticleRevealBackground
z-index 5   HomeReadabilityVeil
z-index 10  首页内容
z-index 30  顶部导航
z-index 40  菜单与提示
z-index 60  模态与沉浸预览
```

### 14.4 可读性处理

首页保留动画的横向流动感，但不能让图案与主标题争夺注意力：

- Canvas 整体显示强度控制在约 78%，不得压低前景文字透明度。
- 在 Canvas 与内容之间增加 `HomeReadabilityVeil`，使用中心较实、边缘渐隐的浅色径向遮罩。
- 遮罩视觉中心位于页面水平中心、垂直约 40% 位置，覆盖主标题和输入区，但不形成可见白色卡片边界。
- 导航 60%、活动页签 78%、输入框 90% 的既定材质保持不变。
- 产品主操作红继续使用 `#E60012`；粒子中的 `#D4351C` 作为插画色保留，不承担按钮或选中语义。
- 不通过文字阴影、描边或额外大白卡片补救可读性。

推荐遮罩起点：

```css
background: radial-gradient(
  ellipse 64% 50% at 50% 40%,
  rgb(250 250 250 / 72%) 0%,
  rgb(250 250 250 / 46%) 46%,
  rgb(250 250 250 / 0%) 78%
);
```

具体透明度允许在真实合成截图中微调，但不得改变“中心安静、边缘保留粒子”的方向。

### 14.5 生命周期与性能

- 路由离开主页或组件卸载时取消 `requestAnimationFrame`，移除事件监听并释放 Canvas 引用。
- 页面进入后台时暂停更新；重新可见后继续，不补算离开期间的时间。
- `prefers-reduced-motion: reduce` 下不启动循环，只显示浅色静态背景或已渲染静态帧。
- 资源加载失败时回退为 `#FAFAFA`，生成主流程必须继续可用。
- 桌面端沿用 `CELL=7`、`DOT_R=2.4`、DPR 上限 2 和最多 60fps。
- 手机端采用低负载配置：`CELL=9`、DPR 上限 1.5、最多 30fps；10 张图和错峰逻辑仍保留。
- resize 使用 200ms 防抖；同一次主页挂载期间保持随机种子稳定，避免窗口轻微变化就完全换一套图案位置。
- 首版不增加鼠标跟随、点击爆炸、滚动驱动或其他交互粒子效果。
- 在 360、768、1440 和 1600px 宽度下分别检查导航、标题、输入文字和模板卡片可读性。


## 15. 响应式规则

### 桌面：≥ 1180px

- 显示完整品牌组合。
- 首页内容宽度按 90vw 展开。
- 中部页签相对视口绝对居中。
- 生成输入区最大 1040px，模板轨道使用更宽主内容。

### 平板：768—1179px

- 品牌可隐藏英文副标，但保留中文名。
- 首页左右边距 24px。
- 主标题最大字号下降，输入框保持单行主结构。
- 模板继续横向滑动。

### 手机：< 768px

- 品牌折叠为 QSAI 标记。
- 顶部导航高度 60px；中部仍保留“主页 / 全部模板”。
- 历史图标保留，不能藏进抽屉。
- 首页左右边距 16px，标题字号 `clamp(34px, 10vw, 46px)`。
- 输入框可增长；按钮组仍在右侧，文字与按钮组最小间距 16px。
- 模板卡片横向滑动，露出下一张卡片边缘。
- 360px 宽度不得出现页面级横向滚动；只有模板轨道自身可横向滚动。

## 16. 配置与组件边界

执行阶段应保持以下职责分离：

- 品牌配置：中文名、英文名和缩写。
- 模板注册配置：稳定 ID、当前展示名、常用排序、预览图、详情内容和工作流映射。
- 页面壳层：背景、顶部导航和通用内容容器。
- 生成组合器：文本、附件、键盘规则、生成状态和错误反馈。
- 模板轨道：排序、选中态、横向滚动和详情跳转。
- 结果详情：继续复用现有结果能力，不复制一套新组件。

模板展示名未来变化时，只修改集中配置；不得修改路由 ID、历史键或生成合同。

## 17. PRD 同步要求

以下结构已同步进主 PRD：

- 一级导航改为全宽顶部导航，不再使用固定侧边导航。
- 首页改为一句话生成入口和常用模板，不再展示最近任务。
- 模板创建不再使用独立页面，模板从主页直接生成。
- 新增独立模板详情页 `/templates/:templateId`。
- 全部模板的“使用模板”和模板详情的“用此模板创作”都返回主页、选中模板并聚焦输入框。
- 历史只从右侧图标进入。
- 生成结果继续使用独立结果页。
- Trae 粒子显影动效只用于主页；离开主页时停止渲染，其他页面使用静态浅色背景。
- 动效以独立 React Canvas 背景组件接入，不通过 iframe 嵌入 Demo，也不依赖项目外绝对资源路径。

原《文旅营销素材生成产品体验与界面设计规范》应同步更新或标记被本设计取代的段落，避免两份文档同时要求侧边导航和顶部导航。

## 18. 验收标准

- [x] 顶部导航使用用户提供的官方红黑 SVG 图形标，桌面 40×40px、移动端 36×36px，无底板、变色或拉伸。
- [x] 页面主操作色为品牌红 `#E60012`；青绿色只作辅助色，不与生成按钮和选中态竞争。
- [x] 顶部导航从左到右全宽铺开，外层没有圆角。
- [x] 顶部导航背景不透明度为 60%，具有毛玻璃模糊，文字和图标保持完全不透明。
- [x] 中部“主页 / 全部模板”相对视口居中。
- [x] 当前页签使用 78% 雾灰，顶部圆、底部与导航底边严格平齐。
- [x] 右侧只有历史图标，并具备提示和无障碍名称。
- [x] 首页不显示最近任务、统计卡片或独立创建页入口。
- [x] 中文主标题采用较克制的两行衬线排版，英文眉题层级清晰；输入提示只在输入框内出现一次。
- [x] 生成输入框背景不透明度为 90%，文字和图标无透明度。
- [x] 输入文字到按钮组为 20px，按钮组内部为 8px；默认高度 60px、纵向内边距 8px，无夸张空白。
- [x] 文本输入聚焦不出现红色外框，按钮的键盘焦点仍清楚可辨。
- [x] 页面只显示 `＋` 和 `↑` 两个主要输入区图标。
- [x] Enter 生成，Shift+Enter 换行，中文输入法确认候选时不误提交。
- [x] 首页默认选中小红书图鉴创作，原创 IP 后置，常用模板顺序与本文档一致。
- [x] 点击模板图片或名称只切换选择；点击小号“详情”进入模板详情。
- [x] 首页主内容使用宽画布，模板轨道不被压缩到标题窄栏。
- [x] 常用模板缩小并下沉至首屏底部附近，中间区域只保留生成输入框。
- [x] 全部模板和模板详情均可把模板带回主页并聚焦输入框。
- [x] 生成成功或部分成功后进入独立结果页，不在主页堆叠结果。
- [x] Trae 粒子显影动效只在主页挂载；进入模板、详情、结果和历史页面后 Canvas 与动画帧均停止。
- [x] 产品动画效果与 `particle-reveal-demo.html` 基准一致，但没有 Demo 说明文字和全局页面样式。
- [x] 动效资源全部来自项目内稳定路径，不依赖 Trae 附件目录或运行时 base64 中间文件。
- [x] Canvas 不拦截点击或滚动，主页标题区域通过渐隐遮罩保持清晰。
- [x] 页面切到后台、开启减少动态效果或动效加载失败时，主页仍可完成输入、选模板和生成。
- [ ] 360、768、1440、1600px 下无阻断性溢出、遮挡或导航偏移。
- [ ] 键盘可完成模板选择、输入、添加图片、生成和进入历史。

## 19. 不在本轮范围

- 重新设计 Trae 已交付的粒子显影视觉，或把它重写为 WebGL、Worker 和交互式粒子系统。
- 模板搜索、筛选、收藏、推荐和市场化运营能力。
- 首页最近任务和数据统计。
- 模型选择、高级参数和提示词编辑。
- 在线图片编辑、后置文字排版和单页重试。
- 登录、云端历史和跨设备同步。
