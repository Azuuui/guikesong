# guikesong介绍

> 最后更新：2026-08-29
> 项目路径：/Users/azu/Documents/guikesong

## 一句话说明项目

面向文旅行业内部人员的模板化营销素材生成 MVP：选择模板、输入一句话并可选上传参考图，一次返回可直接使用的标题、正文、标签和图片，并在用户电脑保留完整历史。

## 背景、目标与范围

当前目标是先搭通前后端和模型调用骨架，使用占位提示词验证完整链路；每个模板包含用户可见文案和视觉规划两套提示词。队友提供正式提示词后，只替换模板配置，不改变核心流程。

首版模板：IP 宣传海报、攻略种草卡、景区氛围大片、人物打卡大片。

## 当前阶段

PRD 已确认，前后端基础工程、四类模板、Mock 生成链路、参考图上传、结果展示、复制下载和本机历史已经完成，可进入运行验收与真实模型接入阶段。

## 项目目录导航

- `docs/superpowers/specs/`：已确认的产品与技术设计
- `docs/superpowers/plans/`：实施计划
- `tasks/`：可勾选的主 PRD、上下文和分阶段执行文件
- `frontend/`：React + TypeScript 前端界面
- `backend/`：后端 API、模板、编排和模型适配器
- `shared/`：前后端共享类型
- `data/reference-assets/`：后端持久参考素材目录（运行时创建，禁止提交 Git）

## 全局关键边界

- 不做登录、跨设备历史、后端生成历史数据库和多轮对话；仅保留当前浏览器 IndexedDB 历史。
- 提示词和模型密钥只放在后端。
- 文案模型和图片模型通过适配器隔离，默认支持占位/mock 模式。
- 文案与视觉规划提示词默认并行执行，视觉规划再拆成页面级图片任务。
- 图片中的文字、艺术字体和排版由图片模型完成，不增加后置字体渲染层。
- 所有模板可选上传最多 4 张 JPG、PNG 或 WebP 参考图，单张不超过 10MB。
- 参考图上传后由后端持久保存，生成完成不自动删除；Demo 只提供管理员手动清理边界。
- 完整历史保存在用户电脑浏览器 IndexedDB，默认最近 20 条，不进入后端业务数据库。
- 部分图片失败时仍返回成功页面和完整文案。
- 不把队友尚未完成的提示词写死在业务流程中。
- 生成结果统一为标题、正文、标签和页面数组，可复制和下载。

## 下一步建议

在新目录运行完整检查和浏览器验收；随后由执行会话接入正式提示词与真实模型 Provider，并保持现有 Mock 模式作为本地回归路径。
