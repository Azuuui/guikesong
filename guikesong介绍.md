# guikesong介绍

> 最后更新：2026-08-30
> 项目路径：/Users/azu/Documents/guikesong

## 一句话说明项目

黔景智作（QianScape AI / QSAI）是面向文旅行业内部人员的模板化营销素材生成 MVP：主页选择工作流、输入一句话并按需上传参考图，后台生成可直接使用的标题、正文、标签和图片，并在用户电脑保留完整历史。

## 背景、目标与范围

当前目标是先搭通前后端和模型调用骨架，使用占位提示词验证完整链路；后续按场景注册独立工作流，场景拥有自己的提示词、确定性脚本规则和输出结构，公共模型 Provider 与基础设施复用。

当前有四个模板入口：`original-ip` 原创 IP 商品化、`xhs-atlas` 小红书图鉴创作、`travel-guide` 目的地手绘攻略与 `ugc-photo-campaign` 照片心情图集。旧四模板（IP 宣传海报、攻略种草卡、景区氛围大片、人物打卡大片）的代码、预览图与后端旧参考图数据已全部删除，旧创建路由返回“没有找到这个模板”。

## 当前阶段

PRD 已确认。四工作流、Mock/真实 Provider、IP Profile 素材库、参考图上传、结果展示、复制下载和本机历史 v2 已完成；主页生成已升级为 24 小时短期后台任务，支持真实阶段进度、跨页面状态圆点、刷新恢复同一 jobId 和终态历史幂等保存。2026-08-30 已用真实 Provider 完成“2个贵州景点”2/2 图片端到端验收。

## 项目目录导航

- `docs/superpowers/specs/`：已确认的产品与技术设计
- `docs/superpowers/plans/`：实施计划
- `tasks/`：可勾选的主 PRD、上下文和分阶段执行文件
- `frontend/`：React + TypeScript 前端界面
- `backend/`：后端 API、模板、编排和模型适配器
- `shared/`：前后端共享类型
- `data/reference-assets/`：后端持久参考素材目录（运行时创建，禁止提交 Git）
- `data/generation-jobs/`：后台任务 24 小时短期快照（运行时创建，禁止提交 Git；不是业务历史）

## 全局关键边界

- 不做登录、跨设备历史、后端生成历史数据库和多轮对话；仅保留当前浏览器 IndexedDB 历史（v2，按工作流存储）。
- 提示词和模型密钥只放在后端；长提示词按场景以 Markdown 模板维护，由脚本确定性填充。
- 文案模型和图片模型通过适配器隔离：`PROVIDER_MODE=mock`（默认）本地回归，`PROVIDER_MODE=real` 走真实模型（智谱 GLM 文案 + 智谱联网搜索 + 中转生图与视觉分析）。
- 不使用自主规划型 Agent；采用场景注册表、独立工作流和公共 Provider。
- 图片中的文字、艺术字体和排版由图片模型完成，不增加后置字体渲染层。
- 参考图上传规则按工作流区分：原创 IP 每次生成上传 1 张产品图；图鉴最多 4 张参考图，只影响画面视觉，不改变清单事实；游客返图 1～7 张投稿照片；手绘攻略不需要参考图；JPG/PNG/WebP 单张不超过 10MB。
- 参考图上传后由后端持久保存，生成完成不自动删除；Demo 只提供管理员手动清理边界。
- 完整历史保存在用户电脑浏览器 IndexedDB，默认最近 20 条，不进入后端业务数据库；从历史重新生成会预填产品描述/选题/目的地/活动主题并恢复本地图。
- 部分图片失败时仍返回成功页面和完整文案。
- 不把队友尚未完成的提示词写死在业务流程中。
- 生成结果统一为标题/候选标题、正文、标签和页面数组，可复制和下载。
- 小红书图鉴工作流额外输出清单 JSON、封面图、动态正文页和发布文案；选题数量规范化到 2～36。
- 原创 IP 工作流支持后台锁定 IP Profile，后续生成自动注入固定 IP 参考图和描述。
- 手绘攻略工作流通过智谱 web-search-pro 联网检索目的地资料，检索失败降级为常识性建议并记录 warning；天数钳制到 1～3 天，素材包额外包含行程 JSON。
- 游客返图工作流一图一海报，逐张海报只吃自己那张照片，单张失败自动重跑且互不影响；结果额外携带整组共同情绪。
- 首页常用顺序固定为 `xhs-atlas` 在前，随后 `original-ip`、`travel-guide`、`ugc-photo-campaign`；模板展示名可变，但路由、历史和生成合同只依赖稳定 ID。
- 原创 IP 已存在锁定档案时可直接在主页生成；未配置档案时保留当前文字和产品图，进入 `/templates/original-ip/create` 完成必要配置。
- Trae 粒子显影动效仅用于主页；离开主页即卸载，模板、结果和历史页面使用静态浅色背景。
- 真实模式下图片与文案经由第三方中转服务处理，中转方可以看到提示词与图片内容，不得提交敏感素材。
- 后台任务创建后可跨页面继续运行并在刷新后恢复；同一浏览器同一时间只允许一个非终态主页任务，创建过程使用跨标签互斥。
- 第一版不取消已提交给第三方的真实请求；关闭页面、删除本机历史或清空浏览器记录不等于取消上游生成。
- IndexedDB 失败不能中断已被后端接受的任务；系统保留轻量 jobId 降级记录继续轮询，完整历史失败时单独提示。

## 下一步建议

保持 Mock 模式作为回归路径，下一步重点是正式部署环境、第三方配额与超时策略、主包拆分，以及四工作流真实图片视觉质量的持续人工验收。
