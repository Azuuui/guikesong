# project002文旅营销素材生成介绍

> 最后更新：2026-08-28
> 项目路径：/Users/azu/Desktop/workspace/project002文旅营销素材生成

## 一句话说明项目

面向文旅行业内部人员的模板化营销素材生成 MVP：选择模板、输入一句话，一次返回可直接使用的标题、正文、标签和图片。

## 背景、目标与范围

当前目标是先搭通前后端和模型调用骨架，使用占位提示词验证完整链路；每个模板包含用户可见文案和视觉规划两套提示词。队友提供正式提示词后，只替换模板配置，不改变核心流程。

首版模板：固定 IP 形象、旅游攻略知识卡片、风景拼图、人物拼图。

## 当前阶段

设计已确认，正在编写可执行 PRD，尚未开始代码实施。

## 项目目录导航

- `docs/superpowers/specs/`：已确认的产品与技术设计
- `docs/superpowers/plans/`：实施计划
- `tasks/`：可勾选的主 PRD、上下文和分阶段执行文件
- `frontend/`：前端界面（待创建）
- `backend/`：后端 API、模板和模型适配器（待创建）
- `shared/`：前后端共享类型（待创建）

## 全局关键边界

- 不做登录、历史记录、数据库和多轮对话。
- 提示词和模型密钥只放在后端。
- 文案模型和图片模型通过适配器隔离，默认支持占位/mock 模式。
- 文案与视觉规划提示词默认并行执行，视觉规划再拆成页面级图片任务。
- 图片中的文字、艺术字体和排版由图片模型完成，不增加后置字体渲染层。
- Demo 不支持参考图片上传；部分图片失败时仍返回成功页面和完整文案。
- 不把队友尚未完成的提示词写死在业务流程中。
- 生成结果统一为标题、正文、标签和页面数组，可复制和下载。

## 下一步建议

先审核 `tasks/prd-文旅营销素材生成.md`，确认后由执行会话按阶段完成基础工程、模板路由、占位生成器、结果展示和端到端冒烟测试。
